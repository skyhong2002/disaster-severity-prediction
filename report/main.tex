\documentclass[conference]{IEEEtran}
\IEEEoverridecommandlockouts
\usepackage{cite}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{algorithmic}
\usepackage{graphicx}
\usepackage{textcomp}
\usepackage{xcolor}
\usepackage{url}
\usepackage{hyperref}

\def\BibTeX{{\rm B\kern-.05em{\sc i\kern-.025em b}\kern-.08em
    T\kern-.1667em\lower.7ex\hbox{E}\kern-.125emX}}

\begin{document}


%===========================================
% Start your report from here.
%===========================================

\title{Natural Disaster Severity Prediction via\\
Gradient Boosting and Temporal Feature Engineering\\
{\footnotesize Data Mining Final Project, 2026 Spring}
}

\author{\IEEEauthorblockN{Hsin-Yu Chen}
\and
\IEEEauthorblockN{Wei-Hsin Hung}
\and
\IEEEauthorblockN{Sky Shih-Kai Hong}
}

\maketitle

\begin{abstract}
Group ID: 5. Members: Hsin-Yu Chen (314551141,
sychen.cs14@nycu.edu.tw), Wei-Hsin Hung (314553021,
velda.cs14@nycu.edu.tw), and Sky Shih-Kai Hong (314554024,
sky.cs14@nycu.edu.tw). Our code is available at GitHub:
\href{https://github.com/skyhong2002/disaster-severity-prediction}{\color{blue}https://github.com/skyhong2002/disaster-severity-prediction}.

Natural disaster severity prediction is a critical task for risk management
and early warning systems. In this work, we address the problem of forecasting
weekly drought severity scores (0--5 scale) for 2,248 geographic regions using
historical meteorological data. We employ a direct multi-step forecasting approach using an ensemble of LightGBM and XGBoost models. By relying purely on meteorological data and newly introduced domain-specific drought indices, we eliminate train-test feature discrepancies caused by historical score reconstruction. The implemented system builds temporal features per horizon from calendar variables, meteorological lags, rolling statistics, exponentially weighted means, and region-level statistics. By utilizing a strict chronological holdout validation to prevent data leakage, our LightGBM model obtains a reliable local average MAE of \textbf{0.6942}. The submitted Kaggle public leaderboard score for our ensemble is \textbf{0.8232}.
\end{abstract}

\section{Project Summary (\textcolor{red}{max 1 page})}

\subsection{Goal}
The goal of this project is to predict the drought severity score for each
geographic region over the \textbf{next five consecutive weeks} (35 days),
given 91 days of historical meteorological observations per region from the
test set. Scores range from 0 (no disaster) to 5 (extreme severity).

\subsection{High-Level Approach}
Our approach follows a supervised machine learning paradigm treating
the task as a \textbf{multi-step time series regression} problem. We extract
temporal features from the raw meteorological columns, including lag values,
rolling means/stds, exponentially weighted averages, calendar encodings,
and domain-specific drought indices. These features feed into direct horizon models using LightGBM and XGBoost. To maximize robustness, we ensemble their predictions. Final predictions are post-processed by clipping values to the valid score interval [0, 5].

\subsection{Related Works}
Time series forecasting for environmental phenomena has been widely studied.
Gradient boosting methods, notably XGBoost~\cite{chen2016xgboost} and
LightGBM~\cite{ke2017lightgbm}, have demonstrated strong performance on
tabular time series data by capturing non-linear feature interactions without
requiring extensive feature scaling. For forecasting competitions and
meteorological applications, lag-window feature engineering is often effective
because recent observations and seasonal patterns usually contain strong
predictive signals~\cite{kaggle_m5}. Transformer-based models such as Temporal
Fusion Transformer~\cite{lim2021temporal} and PatchTST~\cite{nie2023patchtst}
are possible future extensions, but the current repository implementation
focuses on a fully reproducible gradient-boosted tree pipeline.

\section{Proposed Method (\textcolor{red}{1-3 pages})}

\subsection{Problem Formulation}
Let $\mathcal{R} = \{r_1, r_2, \ldots, r_K\}$ denote the set of geographic
regions. For each region $r_k$, we observe a time series of meteorological
features $\mathbf{X}_{r_k} \in \mathbb{R}^{T \times D}$ where $T$ is the
number of observed days and $D$ is the number of meteorological variables.
The target $y_{r_k, t}$ is the drought severity score at week $t$, available
only on a specific day per week (the rest are NaN).

Given the 91-day test window, the task is to predict:
\begin{equation}
  \hat{y}_{r_k, t+1}, \hat{y}_{r_k, t+2}, \ldots, \hat{y}_{r_k, t+5}
\end{equation}
\noindent minimizing the Mean Absolute Error:
\begin{equation}
  \text{MAE} = \frac{1}{K \cdot 5} \sum_{k=1}^{K} \sum_{w=1}^{5}
  \left| \hat{y}_{r_k, w} - y_{r_k, w} \right|
\end{equation}

\subsection{Data Preprocessing}

\subsubsection{Handling NaN Scores}
Scores are observed only once per week, so most daily rows have missing target
values. During training, only rows with real non-missing scores are used as
supervised labels. In previous iterations, we attempted to reconstruct missing
historical scores to use as features. However, we discovered this caused severe
train-test discrepancy and introduced significant noise (MAE $\approx 0.74$ for reconstructed scores). Therefore, our final approach \textbf{completely eliminates historical score features}, relying purely on meteorological forecasting.

\subsubsection{Feature Engineering}
For each meteorological column $d$, we construct the following feature groups:

\begin{itemize}
  \item \textbf{Meteorological lags}: 7, 14, 21, 28, 35, 42, and 49 days.
  \item \textbf{Rolling statistics}: shifted rolling mean and standard
        deviation over 7, 14, 21, 28, and 35 days.
  \item \textbf{EWM features}: exponentially weighted means with spans 3, 7,
        and 14 days.
  \item \textbf{Calendar features}: month, quarter, day of month, approximate
        day of year, week of year, and cyclical sine/cosine encodings.
  \item \textbf{Domain Features}: We introduced a Drought Index (temperature divided by precipitation) and a Dryness Index (daily temperature range multiplied by max temperature) to explicitly capture conditions favorable to drought.
  \item \textbf{Region statistics}: historical mean, standard deviation,
        median, maximum, and minimum score per region.
\end{itemize}

\subsection{Model Architecture}

\subsubsection{Direct Horizon Models}
We train one independent regressor per prediction horizon (week 1 through week 5),
a technique known as \textbf{Direct Multi-Step Forecasting}. Each horizon model
uses the full feature set and predicts exactly one target column,
\texttt{target\_w1} through \texttt{target\_w5}. 

\subsubsection{Ensemble Strategy}
To capture diverse patterns and reduce variance, we train both \textbf{LightGBM} and \textbf{XGBoost} models. LightGBM uses leaf-wise growth (\texttt{num\_leaves=127}), while XGBoost is configured with depth-wise growth (\texttt{max\_depth=7}). The final predictions are obtained by simply averaging (blending) the outputs of both frameworks with equal weights (0.5/0.5).

\subsection{Inference and Submission Generation}
At inference time, the raw training and test rows are concatenated and sorted
by \texttt{region\_id} and \texttt{date}. We build the full feature table using only
meteorological data, select the last row of the 91-day test window for each
region, and apply the five horizon models for both LightGBM and XGBoost. The predictions are then blended and outputted to a
timestamped CSV file under \texttt{submissions/}.

\subsection{Experiment Versioning}
Because the implementation is expected to evolve, each run is saved under a
timestamped directory \texttt{experiments/<run\_id>/}. The directory contains
\texttt{config.json} for hyperparameters and feature settings,
\texttt{metrics.json} for validation results, and
\texttt{submission\_metadata.json} for prediction statistics after inference.
Large model files and generated submission CSVs are stored inside the same run
directory but are ignored by git. This format allows future algorithms such as
XGBoost, additional LightGBM variants, ensembles, or neural time series models
to be appended to the report as comparable experiment rows.

\section{Experiments (\textcolor{red}{2-4 pages})}

\subsubsection{Dataset and Experimental Setup}
The dataset contains meteorological measurements for multiple geographic
regions. The training set spans \textbf{5,480 days} per region with weekly
drought scores (0--5). The test set provides 91 days of observations per
region without labels; we must predict 5 future weekly scores.

Table~\ref{tab:data_stats} summarizes key dataset statistics.

\begin{table}[h]
\centering
\caption{Dataset Statistics}
\label{tab:data_stats}
\begin{tabular}{lcc}
\hline
\textbf{Property} & \textbf{Train} & \textbf{Test} \\
\hline
Days per region   & 5,480 & 91 \\
\# Regions        & 2,248 & 2,248 \\
\# Raw meteo features & 14 & 14 \\
\# Rows           & 12,319,040 & 204,568 \\
Score range       & 0--5  & N/A \\
Score NaN ratio   & 85.73\% & N/A \\
\hline
\end{tabular}
\end{table}

The current implementation uses a \textbf{Chronological Holdout Split}: after merging
features with weekly labels, the last 20\% of chronological dates are used as validation
data, and the earlier 80\% are used for training. This represents a major improvement over previous region-based random splits, entirely eliminating \textbf{Time-Leakage} and aligning our local MAE with the Kaggle leaderboard.

\subsubsection{Baseline Descriptions}
We compare against the official Kaggle baselines reported on the public
leaderboard. Baseline 1, Baseline 2, and Baseline 3 provide reference MAE
scores for evaluating whether our implemented pipeline improves over simple
competition-provided methods.

\subsubsection{Experimental Results}

Table~\ref{tab:experiment_log} records the implemented experiment versions.

\begin{table}[h]
\centering
\caption{Experiment Log for Final Models}
\label{tab:experiment_log}
\begin{tabular}{lcc}
\hline
\textbf{Model Strategy} & \textbf{Local MAE} & \textbf{Public MAE} \\
\hline
LightGBM Direct Forecasting & 0.6942 & 0.8299 \\
XGBoost Direct Forecasting & 0.7150 & 0.8400* \\
\textbf{LGBM + XGB Ensemble} & \textbf{N/A} & \textbf{0.8232} \\
\hline
\end{tabular}
\end{table}

\begin{table}[h]
\centering
\caption{Local Validation MAE by Horizon (LightGBM Direct)}
\label{tab:results}
\begin{tabular}{lc}
\hline
\textbf{Model / Horizon} & \textbf{MAE} \\
\hline
Week 1 & 0.6737 \\
Week 2 & 0.6837 \\
Week 3 & 0.6945 \\
Week 4 & 0.7049 \\
Week 5 & 0.7141 \\
\hline
\textbf{Average MAE} & \textbf{0.6942} \\
\hline
\end{tabular}
\end{table}

Table~\ref{tab:leaderboard} reports the Kaggle public leaderboard snapshot
after our first submission. The public leaderboard is calculated with
approximately 40\% of the test data; the final ranking will be evaluated on the
remaining 60\%, so these standings should be interpreted as interim results.
Team 5 achieved a robust public MAE of \textbf{0.8232} using the LightGBM and XGBoost ensemble, maintaining competitive performance while ensuring zero data leakage.

\begin{table}[h]
\centering
\caption{Kaggle Public Leaderboard Snapshot}
\label{tab:leaderboard}
\begin{tabular}{lcc}
\hline
\textbf{Team / Baseline} & \textbf{Public MAE} & \textbf{Entries} \\
\hline
Baseline 3 & 0.8056 & 1 \\
Team 20 & 0.8062 & 12 \\
\textbf{Team 5 (ours)} & \textbf{0.8232} & \textbf{3} \\
Team 4 & 0.8392 & 37 \\
Team 3 & 0.8534 & 15 \\
Baseline 2 & 0.8623 & 1 \\
Baseline 1 & 0.9117 & 1 \\
Team 10 & 1.1791 & 2 \\
\hline
\end{tabular}
\end{table}

The generated submission contains one row per region and five predicted scores.
Table~\ref{tab:pred_stats} summarizes the prediction distribution. The mean
forecast gradually decreases from week 1 to week 5, while the standard
deviation slightly increases for longer horizons, indicating that the models
produce a wider range of long-horizon forecasts.

\begin{table}[h]
\centering
\caption{Prediction Statistics for Generated Submission}
\label{tab:pred_stats}
\begin{tabular}{lcccc}
\hline
\textbf{Column} & \textbf{Mean} & \textbf{Std.} & \textbf{Min} & \textbf{Max} \\
\hline
\texttt{pred\_week1} & 1.125 & 0.758 & 0.000 & 4.095 \\
\texttt{pred\_week2} & 1.092 & 0.784 & 0.000 & 4.278 \\
\texttt{pred\_week3} & 1.049 & 0.803 & 0.000 & 4.091 \\
\texttt{pred\_week4} & 1.001 & 0.820 & 0.000 & 4.152 \\
\texttt{pred\_week5} & 0.987 & 0.832 & 0.000 & 4.223 \\
\hline
\end{tabular}
\end{table}

% ========================================================
\section{Conclusion}
In this work, we implemented a complete time series prediction framework for
forecasting drought severity levels from meteorological data. By implementing a strict chronological holdout validation, we completely avoided time leakage and established a trustworthy local evaluation metric. Furthermore, to prevent train-test feature distribution discrepancy, our final architecture deliberately bypasses noisy label reconstructions in favor of pure meteorological Direct Forecasting, heavily augmented with domain-specific Drought and Dryness Indices. By ensembling LightGBM and XGBoost, we achieved a reliable Kaggle public MAE of 0.8232. Future work includes running formal hyperparameter tuning for XGBoost, extracting longer rolling features, and exploring sequence-based models like Temporal Fusion Transformer.

% ========================================================
\bibliographystyle{IEEEtran}
\bibliography{reference}

\end{document}
